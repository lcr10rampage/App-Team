# Component Spec — <Component Name>

- **Component name:**
- **Component ID:**
- **Purpose:**
- **Variants:** (e.g. primary/secondary, sizes)
- **Inputs / properties:** (name, type, required, default)
- **Visual states:** (default, hover, active, focus, disabled)
- **Interaction states:** (loading, error, success where relevant)
- **Validation:** (if it takes input)
- **Responsive behavior:**
- **Accessibility behavior:** (role, keyboard, ARIA, focus)
- **Where it is used:** (screens/features)
- **When NOT to use it:** (so it isn't misapplied or duplicated)

> Before specifying a new component, check `memory/reusable-components.md` and `app-build-set/app-system-guide.md` — reuse before inventing.
