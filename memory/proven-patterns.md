# Proven Patterns

> **Seeded source:** the *Proven Patterns* section of `app-build-set/EngineeringMemory.md` (row(delay) entrance, createPortal modals, standalone HTML animations, setTimeout(16) loops, CSS reflow fix, authFetch, shared current_user dep, sessionStorage tokens, bcrypt-direct, bottom-up fill via height, bulk insert on-conflict, etc.). Those are already ★-rated and in force.
>
> This file collects **new** patterns proven through the team process. Manager-approved only.

## Entry format
```
### <Pattern name>
- When to use it:
- How it works:
- Projects where it succeeded:
- Risks:
- When NOT to use it:
- Confidence: ★★★★★
```

## Entries
(Start empty — the foundation holds the seeded set. Add here as the team proves new patterns.)
